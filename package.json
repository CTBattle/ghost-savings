{
  "name": "ghost-savings",
  "version": "0.1.0",
  "private": true,
  "description": "Event-sourced domain engine for Savings, Challenges, and Ghost Debt Mode",
  "type": "module",
  "scripts": {
    "dev": "node --import=dotenv/config ./node_modules/.bin/tsx watch src/api/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/api/server.js",
    "test": "vitest",
    "typecheck": "tsc -p tsconfig.test.json --noEmit"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "fastify": "^5.6.2",
    "firebase": "^12.7.0",
    "firebase-admin": "^13.6.0",
    "plaid": "^41.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "ts-node": "^10.9.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.16"
  }
}
