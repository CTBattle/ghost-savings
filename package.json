{
  "name": "ghost-savings",
  "version": "0.1.0",
  "private": true,
  "description": "Event-sourced domain engine for Savings, Challenges, and Ghost Debt Mode",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/api/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && node dist/api/server.js",
    "test": "vitest",
    "typecheck": "tsc -p tsconfig.test.json --noEmit"
  },
  "dependencies": {
    "fastify": "^5.6.2"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.16"
  }
}
